!!!
%html{lang: :en}
  %head
    %title NoDevent Demo
    = javascript_include_nodevent
    = javascript_include_tag 'application'
    = stylesheet_link_tag "application"
    = csrf_meta_tags
    = yield :head
    = yield :js

  %body
    .top-error
      #error-box.alert.alert-error
        .right.inline-block.float-right
          .icon-repeat.reload{:title => "Reload"}
        %span.message
          Loading...
    .navbar
      .navbar-inner
        .container


          = link_to "NoDeventDemo", root_path, :class => "brand"

    %a{:href => "https://github.com/cconstantine/NoDeventDemo"}
      %img{:style => "position: absolute; top: 0; right: 0; border: 0;",
           :src => "https://s3.amazonaws.com/github/ribbons/forkme_right_green_007200.png",
           :alt=>"Fork me on GitHub"}

    .container-fluid
      .row-fluid
        .span8
          %h3
            Demo app for
            = link_to "NoDevent", "https://github.com/cconstantine/nodevent"
          %p
            Demonstrate how NoDevent could be used in a responsive rails app with backbone integration.  View-source!  Check out the code!
          = yield
        .span3.login_forms
          #logout.float-right
            = button_to "Logout", sessions_path(:format => :json), :method => :delete, :class => "session_form", :remote => true

          #login
            %h2 Login
            = form_tag sessions_path(:format => :json), :method => :post, :remote => true do
              .hidden.session_form
              .inputs
                = text_field_tag  :username, "", :required => true
                = text_field_tag  :password, "", :required => true, :type => :password
                = submit_tag "Login"

            %h2 Register
            = form_for User.new(), :format => :json, :method => :post, :remote => true do |f|
              .hidden.session_form
              .inputs
                = f.text_field :username,  :required => true
                = f.text_field :password,  :required => true, :type => :password
                = submit_tag "Register"